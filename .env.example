# Anthropic API key for the AI oracle agent.
# Without it, the app runs in "Inner Compass" demo mode using the built-in
# deterministic divination engines (I-Ching, numerology, year-pillar cycles).
ANTHROPIC_API_KEY=
